@model LeeftSamen.Portal.Web.Models.Users.PostalCodeViewModel
  
<span class="label label-primary">
    <span class="fa fa-map-marker" aria-hidden="true"></span> @Model.PostalCode    
</span>
@*@if (!Model.IsInActiveCity && !string.IsNullOrWhiteSpace(Model.PostalCode))
{
    <a class="hidden-xs help-icon"
       data-toggle="tooltip"
       data-original-title="@Text.NotActiveInCity"
       data-placement="bottom">
        <span class="glyphicon glyphicon-exclamation-sign"></span>
    </a>
}*@